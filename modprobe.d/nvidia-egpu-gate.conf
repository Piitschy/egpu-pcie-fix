# Block NVIDIA module autoload so egpu-init.service controls probe timing.
# egpu-init.sh uses `modprobe -i` to bypass these install stubs after PCIe setup.
install nvidia /bin/false
install nvidia_modeset /bin/false
install nvidia_uvm /bin/false
install nvidia_drm /bin/false
